import React from 'react';
import { Button } from 'antd';

import './About.css'

const About = () => (
    <div>
        <h2>About</h2>
        <Button type="primary">Primary</Button>
        <div className="about">About Style</div>
    </div>
);

export default About;